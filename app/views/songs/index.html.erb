<tbody>
    <h1> Welcome to CRUD with Validations - The Song Listing App </h1></br>
    <h2> Here are all your songs: </h2>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.release_year %></td>
        <td><%= song.genre %></td>
        <td><%= song.artist_name %></td>
      </tr>
    <% end %>
  </tbody>
