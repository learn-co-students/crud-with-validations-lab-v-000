<h1>Songs</h1>
<tbody>
  <% @songs.each do |song| %>
    <tr>
      <td>
        <%= link_to song.title, song_path(song) %><%= link_to "Delete", song, method: :delete, data: {confirm: "Are you sure?"} %>
      </td>
    <tr>
</tbody>
<% end %>
