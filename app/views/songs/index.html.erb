<tbody>
<% @songs.each do |song| %>
	<tr>
		<th><%= song.title %></th>
		<th><%= song.released %></th>
		<th><%= song.release_year %></th>
		<th><%= song.artist_name %></th>
		<th><%= song.genre %></th>
	</tr>
<% end %>
</tbody>