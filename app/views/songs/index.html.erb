<h1>Song INDEX</h1>
<% @songs.each do |song| %>
  <tbody>
    <tr>
      <%= link_to song.title, song_path(song) %>
      <%= link_to 'Destroy',  song,  method: :delete, data: { confirm: 'Are you sure?' } %>
    </tr>
  </tbody>
<% end %>
    
