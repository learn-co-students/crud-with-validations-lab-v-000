<h1>All Songs</h1>



<%@songs.each do |song|%>
<tbody>
  <tr><%=link_to song.title, song_path(song)%>
  <%= link_to 'Delete', song, method: :delete, data: { confirm: 'Are you sure you want to delete this?' } %>
  </tr>
  
  
</tbody>
<%end%>


