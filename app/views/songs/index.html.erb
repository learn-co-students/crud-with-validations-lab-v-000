<h1>Songs</h1>
<div><%= link_to 'Create Song', new_song_path %></div>
<table>
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td>song.title</td>
      </tr>
    <% end %>
  </tbody>
</table>
