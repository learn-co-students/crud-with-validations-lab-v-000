<h1>Songs</h1>
<table>
  <thead>
    <tr>
      <th>Song</th>
    </tr>
  </thead>
  <tbody>
    <% @songs.each do |song| %>
      <tr><%= link_to song_path(song), song.title %></tr>
    <% end %>
  </tbody>
</table>
