<table>
  <tbody>
      <% @songs.each do |song| %>
        <tr>
        <a href="<%= song_path(song) %>"><%= song.title %></a> - <%= song.artist_name %>
        <br>
        </tr>
      <% end %>
  </tbody>
</table>
