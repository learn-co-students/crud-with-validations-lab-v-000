<h1>All Songs</h1>

<table>
  <thead>
    <tr>
      <th>Songs</th>
    </tr>
  </thead>
  <tbody>
    <% @songs.each do |song| %>
    <tr>
      <%= link_to song.title, song_path(song) %>
    </tr>
    <% end %>
  </tbody>
</table>
