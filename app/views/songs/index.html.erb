<table>
  <thead>
    <tr>
      <th>Artist</th>
      <th>Title</th>
      <th>Year</th>
      <th>Genre</th>
    </tr>
  </thead>

<tbody>
  <% @songs.each do |song| %>
    <tr>
      <td><%= song.artist_name %></td>
      <td><%= song.title %></td>
      <td><%= song.release_year %></td>
      <td><%= song.genre %></td>
    </li>
    </tr>
  <% end %>
</tbody>