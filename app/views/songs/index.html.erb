<h1>Song Library</h1>
<table>
    <tbody>
        <% @songs.each do |song| %>
            <tr><%= song.title %><%= link_to "Delete", song, method: :delete %></tr><br>
        <% end %>
    </tbody>
</table>