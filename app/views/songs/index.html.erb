<h1>Songs</h1>

<div><%= link_to "New Song", new_song_path %></div>

<table>
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <%= song.title %>
      </tr>
    <% end  %>
  </tbody>
</table>
