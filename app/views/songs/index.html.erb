<h1>Song Index</h1> 

<tbody>
<% @songs.each do |song| %>
    <tr>
    <%= song.title %>
    <%= song.release_year %>
    <%= song.artist_name %>
    <%= song.genre %>
    <%= link_to "Delete", song, method: :delete, data: {confirm: "Really?"} %>
    </tr>
<% end %>
</tbody>
