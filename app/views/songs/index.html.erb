<% @songs.each do |song| %>
  <div class="song">
    <tbody>
      <tr>
        <%= song.title %>
      </tr>
    </tbody>
    <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
  </div>
<% end %>
<tbody>
  
</tbody>