<table>
  <tr>
    <th> Title</th>
    <th> Artist </th>
  </tr>
  <tbody>
    <% @songs.each do |song| %>
    <tr>
      <td><%= song.title %></td>
      <td><%= song.artist_name %></td>

    </tr>
    <% end %>
  </tbody>
</table>
