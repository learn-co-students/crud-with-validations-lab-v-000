<h1>Songs</h1>

<%= link_to 'New Song', new_song_path %>

<table>
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td>
          <%= link_to song.title, song %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
