<h1>Index</h1>

<table>
  <thead>
    <tr>
      <th>Songs</th>
    </tr>
  </thead>
  <tbody>
  <% @songs.each do |song| %>
    <tr>
      <td><%= link_to song.title, song_path(song)%></td>
    </tr>
  <% end %>
  </tbody>
</table>