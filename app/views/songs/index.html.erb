<tbody>
  <%= @songs.each do |song| %>
  <tr>
    <%= song.title %>
    <%= song.release_year %>
    <%= song.artist_name %>
    <%= song.genre %>
  </tr>

  <%= link_to 'delete', song, method: :delete, data: { confirm: "Really?" } %>

  <% end %>
</tbody>
