<h1>Here are all songs:</h1>
<%= @songs.each do |song| %>
  <tbody>
    <tr>Title: <%= song.title %>
    Artist: <%= song.artist_name %>
    Genre: <%= song.genre %>
    Released:<%= song.released %>
    Release Date:<%= song.release_year %></tr>
  </tbody>
  <br>
<% end %>
