<%= @songs.each do |song|%>
  <p><%= song.title %></p>
  <p><%= song.released %></p>
  <p><%= song.release_year %></p>
  <p><%= song.artist_name %></p>
  <p><%= song.genre %></p>
  <p><%= song.created_at %></p>
  <p><%= song.updated_at %></p>
<% end %>
