<h1>Edit <%= @song.title %> by <%= @song.artist_name %></h1>

<%= render 'form' %>

<%= link_to 'Cancel Editing', @song %>

<%= link_to 'Delete', @song, method: :delete, data: { confirm: 'For real?'} %>
