<h1><%= @song.title %></h1>

<%= @song.artist_name %><br>
<%= @song.release_year %><br>
<%= @song.genre %><br>

 <%= form_tag song_path(@song.id), method: "delete" %>
  <%= submit_tag "Delete Song" %>