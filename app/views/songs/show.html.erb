<h1><%= @song.title %></h1>
by <strong><%= @song.artist_name %><strong>
  <% if @song.released? %>
  <p>
    Released in
    <strong><%= @song.release_year %></strong>
  </p>
  <% else %>
  <p><i>Not Yet Released. Coming Soon!</i></p>
  <% end %>
<p>
  <strong>Genre:</strong>
    <%= @song.genre %>
</p>

<%= link_to 'Edit', edit_song_path(@song) %>
<%= link_to 'All Songs', songs_path %>
