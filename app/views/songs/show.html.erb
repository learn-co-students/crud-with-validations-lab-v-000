<h1><%= @song.title %></h1>
<h3>Artist: <%= @song.artist_name %></h3>

<% if @song.released? %>
  <h4>Released in: <%= @song.release_year %></h4>
<% end %>

<h4>Genre: <%= @song.genre %></h4>

<br><br>

Edit this Song's info?
<%= link_to 'Edit', edit_song_path(@song) %><br>

Delete this Song?
<%= link_to 'Delete', @song, method: :delete, data: { confirm: "Are you sure? Can't undo." } %><br>

Add New Song?
<%= link_to 'Create Song', new_song_path %><br>

Back to Index?
<%= link_to 'Index', songs_path %>
