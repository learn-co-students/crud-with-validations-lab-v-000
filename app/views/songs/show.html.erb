<p><%= @song.title %></p>
<div><%= @song.released %></div>
<div><%= @song.release_year %></div>
<div><%= @song.artist_name %></div>
<div><%= @song.genre %></div>
<p></p>
<%= link_to 'Back', songs_path %>
<%= link_to 'Edit', edit_song_path(@song) %>

<%= form_tag song_path(@song.id), method: "delete" %>
  <%= submit_tag "Delete #{@song.title}" %>
