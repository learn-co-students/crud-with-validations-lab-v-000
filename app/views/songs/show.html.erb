<h1>Song Show Page</h1>
<h2><%= @song.title %></h2>
<%= @song.artist_name %>
<%= @song.release_year %>
<%= form_tag song_path(@song.id), method: "delete" do %>
  <%= submit_tag "Delete #{@song.title}" %>
<% end %>
