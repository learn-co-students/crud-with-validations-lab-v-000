<h1><%= @song.title %></h1>
<ul>
  <li><%= @song.artist_name %></li>
  <li><%= @song.genre %></li>
</ul>
<%= form_tag song_path(@song), method: "delete" %>
  <%= submit_tag "Delete #{@song.title}" %>
