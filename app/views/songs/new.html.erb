<% if @song.errors.any? %>
    <div id="error_explanation">
        <h2>
            <%= pluralize(@song.error.count, "error") %>
            prohibit this song from being saved:
        </h2>

        <ul>
        <% @song.errors.full_messages.each do |msg| %<
                <li><%= msg %></li>
            <% end %>
        </ul>
    </div>
<% end %>


<%= form_for @song do |f| %>
    <%= f.text_field :title %>
    <%= f.radio_button :releaded %>
    <%= f.text_field :release_year %>
    <%= f.text_field :artist_name %>
    <%= f.text_field :genre %>
    <5= f.submit %>
<% end %>